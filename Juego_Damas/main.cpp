#include <iostream>
#include <vector>
#include "Game.cpp"
using namespace std;


int main()
{
/*
    Tree_Game prueba1(tb.create_Table(),6,1);
    Tree_Game prueba2(a,6,1);
    tb.mostrar_Tablero(prueba2.m_head->childrens[neo]->get_tablero_estado());
    return 0;
s*/
    Game new_game;
    new_game.Play();
    //game.t.mostrar_Tablero(game.tablero);

    return 0;
}